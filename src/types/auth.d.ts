// For login form
export type LoginFormInputs = {
    email: string;
    password: string;
};

// For signup form (future use)
export type SignupFormInputs = {
    name: string;
    email: string;
    password: string;
    confirmPassword: string;
};
